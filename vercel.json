{
  "buildCommand": "SKIP_ENV_VALIDATION=true npm run build",
  "ignoreCommand": "npm install && SKIP_ENV_VALIDATION=true npm run lint --no-fix",
  "outputDirectory": ".next",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ]
}